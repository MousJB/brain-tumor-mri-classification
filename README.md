# brain-tumor-mri-classification
Brain tumor classification from MRI images using PyTorch. Implements custom CNN, fine-tuning with augmentation, and ResNet18 transfer learning. Tested on blurred, noisy, and motion artifact datasets.
